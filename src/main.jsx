import React from 'react';
import ReactDOM from 'react-dom/client';
import RegulatoryTracker from './RegulatoryTracker.jsx';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <RegulatoryTracker />
  </React.StrictMode>
);
